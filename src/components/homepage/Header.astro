---
import { Icon } from 'astro-icon/components'
---

<header class="relative -z-20">
  <div class="container mx-auto px-4 py-28">
    <div
      class="relative -z-10 grid place-content-center gap-6 text-center text-white lg:grid-cols-[1fr_40px_1fr] lg:gap-x-10 lg:gap-y-2"
    >
      <p
        class="text-stroke font-kanit text-7xl font-black uppercase italic tracking-wider text-transparent lg:col-[1/2] lg:row-[1/2] lg:text-end lg:text-8xl"
      >
        Tradie
      </p>
      <p
        class="mx-auto max-w-[30ch] font-kanit text-3xl font-bold uppercase italic lg:col-[1/2] lg:row-[2/3] lg:mx-0 lg:max-w-[20ch] lg:self-end lg:justify-self-end lg:text-end"
      >
        Where tradies come and get discovered online and grow their business
      </p>
      <div
        class="decoration h-3 bg-brand-1 lg:col-[2/3] lg:row-[1/3] lg:h-full"
      >
      </div>
      <p
        class="-mb-6 font-kanit text-7xl font-black uppercase italic tracking-wider text-brand-1 lg:text-start lg:text-8xl"
      >
        Growth
      </p>
      <p
        class="font-kanit text-7xl font-black uppercase italic tracking-wider text-brand-1 lg:text-start lg:text-8xl"
      >
        partner
      </p>
      <p class="mx-auto max-w-[60ch] text-lg lg:col-[1/-1] lg:mt-8 lg:text-xl">
        Whether you’re a custom home builder, run a construction business or
        provide a general trade service such as installing solar panels...
      </p>
      <div class="flex justify-center lg:col-[1/-1] lg:mt-4">
        <a
          href=""
          class="rounded-md bg-white p-4 text-center font-semibold text-black shadow-xl shadow-white/15 transition-all duration-200 hover:-translate-y-1 hover:bg-brand-1 hover:shadow-lg lg:text-lg"
          >We'd love to work with you</a
        >
      </div>
      <div class="absolute right-0 top-[20%] hidden flex-col gap-6 lg:flex">
        <a href="#" class="transition-all duration-200 hover:text-brand-1">
          <Icon name="facebook" size={28} />
        </a>
        <a href="#">
          <Icon
            name="instagram"
            size={28}
            class="transition-all duration-200 hover:text-brand-1"
          />
        </a>
      </div>
    </div>
  </div>

  <style>
    header {
      background: var(--gradient-1), url('../../assets/images/header-image.png');
      background-repeat: no-repeat;
      background-size: cover;
      background-position: center;
    }

    .text-stroke {
      -webkit-text-stroke: 2px var(--color-brand-1);
      text-stroke: 2px var(--color-brand-1);
    }

    .decoration {
      @media screen and (min-width: 1024px) {
        clip-path: polygon(80% 0%, 100% 0%, 20% 100%, 0% 100%);
      }
    }
  </style>
</header>
